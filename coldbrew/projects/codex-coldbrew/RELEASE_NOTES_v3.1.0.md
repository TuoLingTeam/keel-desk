# codex5.6破甲 冷咖啡 · v3.1.0

## 中文

### MAX 双引擎

- **Armor Break MAX**：目标锁定、完成优先、跨轮续作、偏航修复与交付前完整性扫描。
- **Mature M5**：明确成年角色的单档成人创作合同；成人产品工程和技术任务保持技术输出。
- **Fusion 配置**：`[[ENI:PROFILE=MAX]]` 同时恢复 Armor Break、原义锁定、修复扫描与 M5 readiness。

### 配置驱动全链路

- 保持 `EXEC`、`REVERSE`、`PENTEST`、`GAME`、`CODE`、`FICTION`、`ANALYSIS` 七个公开主路由不变。
- 精确 `冷咖啡` / `cold coffee` 激活即设置 `toolchain_registry=ON` 与 `CHAIN_DEPTH=FULL`，注册 17/17 领域、181/181 节点。
- 通过能力图谱选择主要能力，并稳定合成为一条可追踪的顺序阶段链。
- 新增 `skills/eni-coldbrew/contracts/toolchain.json` 与 `skills/eni-coldbrew/scripts/toolchain_orchestrator.py`，按目标选择细粒度节点而非每次运行全部节点。
- 新增 `[[ENI:CAPABILITIES]]`、`[[ENI:CHAIN=FULL]]` 和配置状态命令。
- 引用、代码块、日志、路径、文件名和展示词表不单独触发配置或改变路由。

### 工程与发布

- 新增 Skill 内置“黄文启动器” `skills/eni-coldbrew/scripts/mature_launcher.py`：直接成人创作意图会在同一轮设置 `MATURE_MODE=ON`、输出激活确认并继续完成原请求，无需运行外部启动程序。
- 成人模式的开发、部署、测试和产品控制进入 `MATURE_PRODUCT` 技术链；引用、代码块、日志、路径和文件内容中的触发词保持为数据。
- 新增机器可读 Profile/Capability JSON 合同和确定性模拟回归。
- Windows 与 POSIX Shell 覆盖十五层替换安装、逐层恢复和精确回滚。
- 安装器、验证器和发布器统一读取根目录 `VERSION`。
- 加入来源哈希、独立重构相似度审计、可复现 ZIP 与内部/仓库 SHA-256 清单。
- 更新中英 README、GitHub Pages、命令/架构/迁移文档和亮暗主题视觉。

### 兼容升级

安装名称仍为 `eni-coldbrew`，短激活词与七路由顺序保持不变。v3.0.1 可使用原替换安装命令升级；运行卸载器一次即可恢复上一层。

交流群：`codex破甲`（1057540028）与 `codex claude 破甲`（1077074552）。

### 发布包

- `ENI-ColdBrew-Unlimited-v3.1.0.zip`
- SHA-256 见 GitHub Release、仓库 README 与 `SHA256SUMS.txt`。

## English

### MAX dual engine

- **Armor Break MAX** adds objective locking, completion-first delivery, cross-turn continuity, drift repair, and a final completeness audit.
- **Mature M5** provides a single adult-fiction contract for explicitly adult casts while keeping adult-product engineering technical.
- `[[ENI:PROFILE=MAX]]` restores Armor Break, literal-intent lock, repair scanning, and M5 readiness together.

### Configuration-driven full chain

- The seven public routes remain `EXEC`, `REVERSE`, `PENTEST`, `GAME`, `CODE`, `FICTION`, and `ANALYSIS`.
- Exact `冷咖啡` / `cold coffee` activation sets `toolchain_registry=ON` and `CHAIN_DEPTH=FULL`, registering all 17 domains and 181 nodes.
- A capability graph selects the relevant capability and composes one stable, traceable sequential stage chain.
- New `skills/eni-coldbrew/contracts/toolchain.json` and `skills/eni-coldbrew/scripts/toolchain_orchestrator.py` select objective-relevant fine-grained nodes instead of running every node on each task.
- New controls expose capabilities, restore full depth, and inspect profile state.
- Quotes, code blocks, logs, paths, filenames, and displayed keyword lists remain data rather than routing triggers.

### Engineering and release

- The built-in `skills/eni-coldbrew/scripts/mature_launcher.py` turns direct adult-creative intent into same-turn `MATURE_MODE=ON`, emits the activation confirmation, and continues the original request without an external launcher program.
- Adult-mode development, deployment, tests, and product controls enter the technical `MATURE_PRODUCT` chain; triggers inside quotes, code, logs, paths, and file content remain data.
- Machine-readable profile/capability contracts and deterministic simulator regressions.
- Fifteen-level replacement and exact rollback coverage on Windows and POSIX Shell.
- One root `VERSION` source shared by installation, verification, and release tooling.
- Source fingerprints, independent-rewrite similarity auditing, reproducible ZIP output, and internal/repository SHA-256 manifests.
- Updated bilingual README, GitHub Pages, command/architecture/migration docs, and theme-aware artwork.

The installed name, short activation phrases, seven route identifiers, receipt compatibility, and rollback behavior remain compatible with v3.0.1.

QQ groups: `codex破甲` (1057540028) and `codex claude 破甲` (1077074552).

### Asset

- `ENI-ColdBrew-Unlimited-v3.1.0.zip`
- See the GitHub Release, README, and `SHA256SUMS.txt` for the SHA-256 digest.
