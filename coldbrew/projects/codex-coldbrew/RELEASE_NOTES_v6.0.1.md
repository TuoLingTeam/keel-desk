# Codex 破甲 · ColdBrew Studio v6.0.1

## Compatibility and responsiveness repair

- Ordinary requests now enter the full execution chain immediately after the
  ColdBrew layer is installed.
- `冷咖啡` remains the canonical command for displaying the complete startup
  document.
- `cold coffee` and `[[ENI:PROFILE=MAX]]` are restored as compatibility entries
  for existing workflows.
- Codex profile-engine, router, brain-pack and CLI regression tests cover these
  states.

The release retains reversible deployment, ownership checks, independent skill
layers, deterministic source archives and verification tooling.
